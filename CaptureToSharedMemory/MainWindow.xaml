<Window x:Class="CaptureToSharedMemory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CaptureToSharedMemory"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MainWindow" Height="500" Width="850">
    <Window.Resources>
        <Storyboard x:Key="rectAnimation">
            <DoubleAnimation Storyboard.TargetName="rect"
                             Storyboard.TargetProperty="(Canvas.Left)"
                             To="600"
                             Duration="0:0:2"
                             RepeatBehavior="Forever"/>     
        </Storyboard>
    </Window.Resources>
    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource rectAnimation}" />
        </EventTrigger>
    </Window.Triggers>
    <Canvas Name="Root" Height="450" Width="800" Background="White" ClipToBounds="True">
        <Rectangle Name="rect" Canvas.Left="0" Canvas.Top="100" Width="200" Height="100" >
            <Rectangle.Fill>
                <SolidColorBrush Color="Red" />
            </Rectangle.Fill>
        </Rectangle>
    </Canvas>
</Window>
